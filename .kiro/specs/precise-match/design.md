# 精确匹配功能 - 设计文档

## 一、差异化定位

| 维度 | 快速配方（首页） | 精确匹配（加号入口） |
|------|-----------------|---------------------|
| 定位 | 快速上手，简单易用 | 专业深度，精准推荐 |
| 参数数量 | 4-5个基础参数 | 10+个专业参数 |
| 分析展示 | 无 | ArkWeb+ECharts图表 |
| 匹配评分 | 无 | 0-100分精确评分 |
| 换一换 | 随机换 | 询问原因后针对性换 |
| 分析过程 | 无 | 5步动画展示 |
| 备选方案 | 无 | 2-3个对比方案 |

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                   PreciseMatchPage                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  基础参数区                                           │  │
│  │  • 目标鱼种（横向滚动，按水域筛选）                    │  │
│  │  • 水域环境（河流/湖泊/池塘/水库）                    │  │
│  │  • 季节天气（温度滑块 + 四季选择）                    │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  高级选项区（可折叠）                                 │  │
│  │  • 水深：浅水(<1m) / 中层(1-3m) / 深水(>3m)          │  │
│  │  • 水质：清澈 / 微浑 / 浑浊 / 肥水                    │  │
│  │  • 光照：强光 / 阴天 / 晨昏 / 夜钓                    │  │
│  │  • 风力：无风 / 微风 / 中风 / 大风                    │  │
│  │  • 饵料状态：雾化快 / 雾化慢 / 粘软 / 干散            │  │
│  │  • 味型强度：清淡 / 适中 / 浓郁 / 刺激                │  │
│  │  • 目标体型：小型(<1斤) / 中型(1-5斤) / 大型(>5斤)   │  │
│  └──────────────────────────────────────────────────────┘  │
│                    [✦ 开始精确分析]                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   AnalysisAnimation                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Step 1: 参数解析中...        ████████░░ 80%         │  │
│  │  Step 2: 鱼种习性匹配...      ██████░░░░ 60%         │  │
│  │  Step 3: 原料智能筛选...      ████░░░░░░ 40%         │  │
│  │  Step 4: 配比优化计算...      ██░░░░░░░░ 20%         │  │
│  │  Step 5: 生成最优方案...      ░░░░░░░░░░ 0%          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   PreciseResultPage                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  顶部：返回 | 精确匹配结果 | [换一换]                 │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  匹配度评分卡片                                       │  │
│  │  ┌────────────┐                                      │  │
│  │  │    92     │  匹配度评分                           │  │
│  │  │   /100    │  "非常适合当前钓况"                   │  │
│  │  └────────────┘                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  分析仪表盘 (ArkWeb + ECharts)                       │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐                │  │
│  │  │ 雷达图  │ │  饼图   │ │ 条形图  │                │  │
│  │  │ 维度评分│ │ 原料配比│ │ 参数权重│                │  │
│  │  └─────────┘ └─────────┘ └─────────┘                │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  配方详情卡片                                         │  │
│  │  • 配方名称 + 标签                                   │  │
│  │  • 原料列表（名称、用量、配比%）                     │  │
│  │  • 制作步骤                                          │  │
│  │  • 使用建议                                          │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  备选方案（横向滑动 2-3 个）                         │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 三、核心组件设计

### 3.1 PreciseMatchPage（参数输入页）

**文件**: `entry/src/main/ets/pages/PreciseMatchPage.ets`

```typescript
// 状态定义
@State selectedFish: string = '';
@State selectedWater: RecipeWaterType = 'river';
@State selectedSeason: RecipeSeason = 'spring';
@State temperature: number = 24;
@State showAdvanced: boolean = false;

// 高级选项状态
@State waterDepth: 'shallow' | 'medium' | 'deep' = 'medium';
@State waterQuality: 'clear' | 'slight_muddy' | 'muddy' | 'fertile' = 'clear';
@State lightCondition: 'bright' | 'cloudy' | 'dawn_dusk' | 'night' = 'bright';
@State windCondition: 'none' | 'light' | 'medium' | 'strong' = 'none';
@State baitState: 'fast_dissolve' | 'slow_dissolve' | 'sticky' | 'dry_loose' = 'fast_dissolve';
@State flavorIntensity: 'light' | 'medium' | 'strong' | 'intense' = 'medium';
@State targetFishSize: 'small' | 'medium' | 'large' = 'medium';
```

### 3.2 PreciseMatchEngine（精确匹配引擎）

**文件**: `entry/src/main/ets/services/engine/PreciseMatchEngine.ets`

```typescript
// 输入参数接口
export interface PreciseMatchInput {
  // 基础参数
  targetFish: string;
  waterType: RecipeWaterType;
  season: RecipeSeason;
  temperature: number;
  
  // 高级参数
  waterDepth: WaterDepth;
  waterQuality: WaterQuality;
  lightCondition: LightCondition;
  windCondition: WindCondition;
  baitState: BaitState;
  flavorIntensity: FlavorIntensity;
  targetFishSize: FishSize;
}

// 输出结果接口
export interface PreciseMatchResult {
  recipe: GeneratedRecipe;
  matchScore: number;           // 0-100 匹配度
  dimensionScores: {
    attraction: number;         // 诱鱼性
    palatability: number;       // 适口性
    state: number;              // 状态
    retention: number;          // 留鱼性
    costEffective: number;      // 性价比
  };
  parameterWeights: Array<{     // 参数权重
    name: string;
    weight: number;
    impact: string;
  }>;
  analysisSteps: Array<{        // 分析步骤
    step: number;
    title: string;
    description: string;
    keyData: string;
  }>;
  alternatives: GeneratedRecipe[];  // 备选方案
}
```

### 3.3 AnalysisDashboard（分析仪表盘）

**文件**: `entry/src/main/ets/components/AnalysisDashboard.ets`

使用 ArkWeb 加载 ECharts 图表：

```typescript
@Component
export struct AnalysisDashboard {
  @Prop dimensionScores: DimensionScores;
  @Prop ingredientRatios: IngredientRatio[];
  @Prop parameterWeights: ParameterWeight[];
  
  private webController: webview.WebviewController = new webview.WebviewController();
  
  build() {
    Column({ space: 16 }) {
      // 雷达图 - 五维评分
      Web({ src: $rawfile('charts/radar.html'), controller: this.webController })
        .height(220)
        .onPageEnd(() => {
          this.updateRadarChart();
        })
      
      // 饼图 - 原料配比
      Web({ src: $rawfile('charts/pie.html'), controller: this.pieController })
        .height(200)
      
      // 条形图 - 参数影响权重
      Web({ src: $rawfile('charts/bar.html'), controller: this.barController })
        .height(160)
    }
  }
}
```

### 3.4 FeedbackDialog（反馈对话框）

**文件**: `entry/src/main/ets/components/FeedbackDialog.ets`

```typescript
// 反馈选项
const FEEDBACK_OPTIONS = [
  { id: 'flavor_dislike', label: '味型不喜欢', icon: '👃' },
  { id: 'ingredient_hard', label: '原料难获取', icon: '🛒' },
  { id: 'state_wrong', label: '状态不合适', icon: '💧' },
  { id: 'want_more_fishy', label: '想要更腥', icon: '🐟' },
  { id: 'want_more_fragrant', label: '想要更香', icon: '🌸' },
  { id: 'want_lighter', label: '想要更淡', icon: '💨' },
  { id: 'other', label: '其他原因', icon: '💬' }
];
```

## 四、ECharts 图表配置

### 4.1 雷达图 (radar.html)

```html
<!DOCTYPE html>
<html>
<head>
  <script src="echarts.min.js"></script>
  <style>body{margin:0;padding:0;}</style>
</head>
<body>
  <div id="chart" style="width:100%;height:220px;"></div>
  <script>
    const chart = echarts.init(document.getElementById('chart'));
    
    function updateData(scores) {
      chart.setOption({
        radar: {
          indicator: [
            { name: '诱鱼性', max: 100 },
            { name: '适口性', max: 100 },
            { name: '状态', max: 100 },
            { name: '留鱼性', max: 100 },
            { name: '性价比', max: 100 }
          ],
          radius: '65%'
        },
        series: [{
          type: 'radar',
          data: [{
            value: [scores.attraction, scores.palatability, 
                    scores.state, scores.retention, scores.costEffective],
            areaStyle: { color: 'rgba(230, 162, 60, 0.3)' },
            lineStyle: { color: '#E6A23C' }
          }]
        }]
      });
    }
  </script>
</body>
</html>
```

### 4.2 饼图 (pie.html)

```javascript
const option = {
  series: [{
    type: 'pie',
    radius: ['40%', '70%'],
    label: { formatter: '{b}: {d}%' },
    data: [
      { value: 40, name: '基础饵', itemStyle: { color: '#E6A23C' } },
      { value: 25, name: '主攻饵', itemStyle: { color: '#67C23A' } },
      { value: 20, name: '状态饵', itemStyle: { color: '#409EFF' } },
      { value: 15, name: '添加剂', itemStyle: { color: '#F56C6C' } }
    ]
  }]
};
```

## 五、文件结构

```
entry/src/main/ets/
├── pages/
│   ├── PreciseMatchPage.ets      # 精确匹配参数输入页
│   └── PreciseResultPage.ets     # 精确匹配结果展示页
├── components/
│   ├── AnalysisDashboard.ets     # 分析仪表盘（ArkWeb图表）
│   ├── FeedbackDialog.ets        # 换一换反馈对话框
│   └── AnalysisAnimation.ets     # 分析过程动画
├── services/engine/
│   └── PreciseMatchEngine.ets    # 精确匹配引擎
└── common/types/
    └── PreciseMatchTypes.ets     # 类型定义

entry/src/main/resources/rawfile/
└── charts/
    ├── echarts.min.js            # ECharts库
    ├── radar.html                # 雷达图
    ├── pie.html                  # 饼图
    └── bar.html                  # 条形图
```

## 六、数据流程

```
用户输入参数 → PreciseMatchPage
       │
       ▼
参数验证 → 显示分析动画 (2-3秒)
       │
       ▼
PreciseMatchEngine.analyze()
  ├── 1. 解析参数权重
  ├── 2. 匹配鱼种习性
  ├── 3. 筛选适合原料
  ├── 4. 计算最优配比
  └── 5. 生成评分和方案
       │
       ▼
PreciseResultPage 展示结果
  ├── 匹配度评分
  ├── ECharts图表
  ├── 配方详情
  └── 备选方案
       │
       ▼
用户点击"换一换" → FeedbackDialog
       │
       ▼
根据反馈重新计算 → 更新结果
```
