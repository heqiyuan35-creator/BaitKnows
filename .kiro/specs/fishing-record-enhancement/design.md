# Design Document: 渔获记录功能增强

## Overview

增强渔获记录功能，包括完善记录列表页(FishingRecordPage)、添加记录页(AddRecordPage)、记录详情页(RecordDetailPage)，以及统计分析和筛选搜索功能。所有数据通过 FishingRecordService 管理，使用 Preferences 持久化存储。

## Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Pages Layer                            │
├─────────────────────────────────────────────────────────────┤
│  FishingRecordPage    AddRecordPage    RecordDetailPage     │
│  (记录列表+统计)       (添加记录)        (详情/编辑)          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Components Layer                         │
├─────────────────────────────────────────────────────────────┤
│  StatCard          RecordCard         FilterSheet           │
│  (统计卡片)         (记录卡片)         (筛选面板)             │
│  EmptyState        DatePicker         FishSelector          │
│  (空状态)          (日期选择)          (鱼种选择)             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Services Layer                           │
├─────────────────────────────────────────────────────────────┤
│  FishingRecordService                UserService            │
│  - load()                            - onRecordAdded()      │
│  - addRecord()                       - onRecordDeleted()    │
│  - updateRecord()                    - recalculateStats()   │
│  - deleteRecord()                                           │
│  - getStatistics()                                          │
│  - filterByFish()                                           │
│  - filterByDateRange()                                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Storage Layer                            │
├─────────────────────────────────────────────────────────────┤
│  StorageService (Preferences)                               │
│  Key: 'fishing_records' -> FishingRecord[]                  │
└─────────────────────────────────────────────────────────────┘
```

## Components and Interfaces

### 1. FishingRecordPage 记录列表页

主要展示统计数据和记录列表。

```typescript
@Entry
@Component
struct FishingRecordPage {
  @State records: FishingRecord[] = [];
  @State statistics: RecordStatistics = new RecordStatistics();
  @State isLoading: boolean = true;
  @State filterFish: string = '';        // 鱼种筛选
  @State filterStartDate: number = 0;    // 开始日期
  @State filterEndDate: number = 0;      // 结束日期
  @State showFilterSheet: boolean = false;
  
  // 生命周期
  aboutToAppear(): void;
  onPageShow(): void;
  
  // 方法
  loadData(): Promise<void>;
  applyFilter(): void;
  navigateToAdd(): void;
  navigateToDetail(recordId: string): void;
}
```

### 2. AddRecordPage 添加记录页

表单页面，用于新增渔获记录。

```typescript
@Entry
@Component
struct AddRecordPage {
  @State date: number = Date.now();
  @State location: string = '';
  @State weather: string = '晴朗';
  @State fishName: string = '';
  @State weight: string = '';
  @State count: string = '';
  @State maxSingle: string = '';
  @State recipeId: string = '';
  @State recipeName: string = '';
  @State notes: string = '';
  @State isSaving: boolean = false;
  
  // 方法
  validateForm(): boolean;
  saveRecord(): Promise<void>;
  selectRecipe(): void;
  calculateAvgWeight(): void;
}
```

### 3. RecordCard 记录卡片组件

用于在列表中展示单条记录。

```typescript
@Component
struct RecordCard {
  record: FishingRecord;
  onClick: () => void;
  onDelete?: () => void;
  
  // 显示内容
  // - 日期 + 天气图标
  // - 钓点位置
  // - 鱼种 + 数量 + 重量
  // - 配方名称（如有）
}
```

### 4. StatCard 统计卡片组件

用于展示统计数据。

```typescript
@Component
struct StatCard {
  icon: string;
  title: string;
  value: string;
  unit: string;
  color?: string;
}
```

### 5. FilterSheet 筛选面板

底部弹出的筛选面板。

```typescript
@Component
struct FilterSheet {
  @Link visible: boolean;
  @Link filterFish: string;
  @Link filterStartDate: number;
  @Link filterEndDate: number;
  onApply: () => void;
  onReset: () => void;
}
```

## Data Models

### FishingRecord 渔获记录

```typescript
// 已存在于 UserTypes.ets
class FishingRecord {
  id: string = '';              // 唯一ID
  date: number = 0;             // 日期时间戳
  location: string = '';        // 钓点位置
  weather: string = '';         // 天气
  fishName: string = '';        // 鱼种名称
  weight: number = 0;           // 总重量(kg)
  count: number = 0;            // 数量(尾)
  maxSingle: number = 0;        // 最大单尾(kg)
  recipeId: string = '';        // 关联配方ID
  recipeName: string = '';      // 配方名称
  notes: string = '';           // 备注
  createTime: number = 0;       // 创建时间
  updateTime: number = 0;       // 更新时间
}
```

### RecordStatistics 统计数据

```typescript
// 已存在于 FishingRecordService.ets
class RecordStatistics {
  totalWeight: number = 0;      // 总重量
  totalCount: number = 0;       // 总尾数
  maxSingle: number = 0;        // 最大单尾
  monthTrips: number = 0;       // 本月出钓
  totalTrips: number = 0;       // 总出钓次数
}
```

## Page Flow

```
┌──────────────────┐
│  MainTabPage     │
│  (日记Tab)       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐     点击添加      ┌──────────────────┐
│ FishingRecordPage│ ───────────────► │  AddRecordPage   │
│  - 统计卡片       │                  │  - 表单输入       │
│  - 记录列表       │                  │  - 保存记录       │
└────────┬─────────┘                  └────────┬─────────┘
         │                                      │
         │ 点击记录                              │ 保存成功
         ▼                                      │
┌──────────────────┐                           │
│ RecordDetailPage │ ◄─────────────────────────┘
│  - 详情展示       │
│  - 编辑/删除      │
└──────────────────┘
```

## UI Layout

### FishingRecordPage 布局

```
┌─────────────────────────────────────┐
│  ← 渔获记录                    🔍   │  Header
├─────────────────────────────────────┤
│  ┌───────┐ ┌───────┐ ┌───────┐     │
│  │总渔获 │ │最大单尾│ │本月出钓│     │  统计卡片
│  │12.5kg │ │ 3.2kg │ │  8次  │     │
│  └───────┘ └───────┘ └───────┘     │
├─────────────────────────────────────┤
│  12月                               │
│  ┌─────────────────────────────┐   │
│  │ 📅 12月20日 周五    ☀️ 晴朗  │   │
│  │ 📍 青山湖钓场               │   │  记录卡片
│  │ 🐟 鲫鱼 × 15尾    2.3kg     │   │
│  │ 🧪 野钓鲫鱼配方             │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 📅 12月18日 周三    ⛅ 多云  │   │
│  │ 📍 东湖水库                 │   │
│  │ 🐟 鲤鱼 × 3尾     4.5kg     │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│              [+ 添加记录]           │  底部按钮
└─────────────────────────────────────┘
```

### AddRecordPage 布局

```
┌─────────────────────────────────────┐
│  ← 添加渔获                   保存   │  Header
├─────────────────────────────────────┤
│  📅 日期                            │
│  ┌─────────────────────────────┐   │
│  │ 2024年12月22日              ▼│   │
│  └─────────────────────────────┘   │
│                                     │
│  📍 钓点位置                        │
│  ┌─────────────────────────────┐   │
│  │ 请输入钓点名称                │   │
│  └─────────────────────────────┘   │
│                                     │
│  🌤️ 天气                           │
│  [晴朗] [多云] [阴天] [小雨] [大风] │
│                                     │
│  🐟 鱼种                            │
│  ┌────┐┌────┐┌────┐┌────┐┌────┐   │
│  │鲫鱼││鲤鱼││草鱼││青鱼││其他│   │
│  └────┘└────┘└────┘└────┘└────┘   │
│                                     │
│  ⚖️ 重量 (kg)        🔢 数量 (尾)   │
│  ┌──────────┐       ┌──────────┐   │
│  │          │       │          │   │
│  └──────────┘       └──────────┘   │
│                                     │
│  🏆 最大单尾 (kg)                   │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                     │
│  🧪 使用配方 (可选)                 │
│  ┌─────────────────────────────┐   │
│  │ 点击选择配方                 ▶│   │
│  └─────────────────────────────┘   │
│                                     │
│  📝 备注                            │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  │                             │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

## Error Handling

| 场景 | 处理方式 |
|------|----------|
| 数据加载失败 | 显示错误提示，提供重试按钮 |
| 保存失败 | 显示Toast提示"保存失败，请重试" |
| 删除失败 | 显示Toast提示"删除失败" |
| 必填字段为空 | 高亮显示未填字段，提示"请填写必填信息" |
| 数值格式错误 | 提示"请输入有效的数字" |

## Testing Strategy

### 单元测试
- FishingRecordService 的增删改查方法
- RecordStatistics 统计计算逻辑
- 筛选过滤逻辑

### UI测试
- 记录列表正确显示
- 添加记录表单验证
- 编辑和删除功能
- 筛选功能

### 集成测试
- 添加记录后列表更新
- 删除记录后统计更新
- 数据持久化验证
